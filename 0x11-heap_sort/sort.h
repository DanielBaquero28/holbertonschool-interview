#ifndef H_HEAP_SORT
#define H_HEAP_SORT

/** Libraries */
#include <stdio.h>
#include <stddef.h>
#include <stdlib.h>

/** Prototypes */
void heap_sort(int *array, size_t size);
void print_array(const int *array, size_t size);

#endif
