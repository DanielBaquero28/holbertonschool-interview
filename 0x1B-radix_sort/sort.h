#ifndef H_RADIX_SORT
#define H_RADIX_SORT

/** Libraries */
#include <stdio.h>
#include <stdlib.h>
#include <stddef.h>

/** Prototypes */
void print_array(const int *array, size_t size);
void radix_sort(int *array, size_t size);
#endif
